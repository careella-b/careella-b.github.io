This project was created with Create React App.

In the command line in the project directory, you can run:

 `npm start`

Runs the app locally
Should run automatically. If not open [http://localhost:3000](http://localhost:3000) to view it in your browser.



